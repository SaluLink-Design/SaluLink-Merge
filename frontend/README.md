# SaluLink Operations (frontend)

Next.js app for chronic care workflows, Supabase auth, and Authi-assisted coding.

## Setup

```bash
cp .env.example .env.local
# Set PYTHON_BACKEND_URL=http://localhost:8000
```

From monorepo root: `npm install` then `npm run dev:web` or `npm run dev`.
